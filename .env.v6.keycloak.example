# Copy this file to .env.v6.keycloak and fill placeholders.

# Superset core
SUPERSET_ENV=production
SUPERSET_LOAD_EXAMPLES=no
SUPERSET_ADMIN_USERNAME=admin
SUPERSET_ADMIN_EMAIL=admin@superset.local
SUPERSET_ADMIN_PASSWORD=admin
SUPERSET_ADMIN_FIRSTNAME=Admin
SUPERSET_ADMIN_LASTNAME=User
SUPERSET_SECRET_KEY=CHANGE_ME_SUPERSET_SECRET_KEY_LONG_RANDOM

# Enable OAuth only in this staging instance
ENABLE_KEYCLOAK_OAUTH=true

# Keycloak placeholders
KEYCLOAK_BASE_URL=https://<KEYCLOAK_HOST>
KEYCLOAK_REALM=<KEYCLOAK_REALM>
KEYCLOAK_CLIENT_ID=superset
KEYCLOAK_CLIENT_SECRET=CHANGE_ME_KEYCLOAK_CLIENT_SECRET

# Superset role sync
AUTH_USER_REGISTRATION_ROLE=Gamma
AUTH_ROLES_SYNC_AT_LOGIN=true

# Proxy / security
ENABLE_PROXY_FIX=true
WTF_CSRF_ENABLED=false
TALISMAN_ENABLED=false
X_FRAME_OPTIONS=ALLOWALL
CORS_ALLOWED_ORIGINS=http://localhost:17790,https://<SUPERSET_PUBLIC_HOST>,https://<TOOLBOX_HOST>

# Optional tuning
SUPERSET_WEBSERVER_TIMEOUT=300
SQLLAB_ASYNC_TIME_LIMIT_SEC=300


