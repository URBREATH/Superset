# .env.jobs ? Variabili d'ambiente per urbreath-ds-jobs
# ATTENZIONE: non committare questo file in repository pubblici.

# ?? Jasypt: chiave di decifratura credenziali (OBBLIGATORIA) ?????????????????
JASYPT_PASSWORD=3DhnYMX8IuJpTASR

# Backoffice security and web port
BACKOFFICE_TOKEN=change-this-token
BACKOFFICE_PORT=18180

# DB applicativo condiviso con stack Superset (service otherdb_v6)
APP_DB_HOST=otherdb_v6
APP_DB_PORT=5432
APP_DB_NAME=urbreath_dev
APP_DB_USER=external_user
APP_DB_PASSWORD=external_pass

# Scheduler: configurazione iniziale in application.yaml.
# Le modifiche cron/enabled si fanno da backoffice in runtime.
