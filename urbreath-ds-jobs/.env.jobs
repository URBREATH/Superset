# .env.jobs – Variabili d'ambiente per urbreath-ds-jobs
# ATTENZIONE: non committare questo file in repository pubblici.

# ?? Jasypt: chiave di decifratura credenziali (OBBLIGATORIA) ?????????????????
JASYPT_PASSWORD=3DhnYMX8IuJpTASR

# ?? Abilitazione job (true/false) ?????????????????????????????????????????????
# Impostare true solo per i job che si vuole attivare in questo ambiente.
TRAFFIC_ENABLED=false
TEMPERATURE_ENABLED=false
PRECIPITATION_ENABLED=false
WMS_ENABLED=false
FROST_CONFIG_ENABLED=false
FROST_IMPORT_ENABLED=false

# ?? Override cron (opzionale) ?????????????????????????????????????????????????
# Se non impostati, si usano i valori di default dell'application.yaml
# WMS_CRON=0 0/5 * * * *
# FROST_IMPORT_CRON=0 0/20 * * * *

